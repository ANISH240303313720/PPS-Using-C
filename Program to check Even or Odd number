#include <stdio.h>
 int main()
{
  int num1,rem;
  printf("Enter Number to check it:");
  scanf("%d",& num1);
  rem= (num1%2);
   if(rem==0)
  {
     printf("Even");
  }
else
  {
      print("Odd");
  }
  return 0;
} 
